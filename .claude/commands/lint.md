# Lint

Run ESLint and TypeScript type check on the project (same checks as pre-push hook).

```bash
pnpm lint
```

If there are errors, analyze them and fix them one by one. After fixing, run `pnpm lint` again to confirm all errors are resolved.
